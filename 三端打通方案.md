# 悟空-钉钉-Obsidian 三端打通方案

## 一、现状分析

### 1.1 当前系统状态

**悟空工作空间** (`/home/wuying/.kinto-agent/workspace/projects/工作总结`)
- `work_logs/`：工作日志目录（格式：YYYY-MM-DD.md）
- `weekly_summaries/`：周总结目录（格式：YYYY-WW_周总结.md）
- `monthly_summaries/`：月报目录（格式：YYYY-MM_月总结.md）
- `memory/`：记忆文件目录
- `artifacts/`：交付物目录
- `knowledge_base.md`：知识库文档

**钉钉文档现状**
- 已存在"2026-07-03 工作日志"等按日期命名的文档
- 已有"前台中心28周总结29周计划"等周总结文档
- 已有"渠道中心6月总结与7月计划"等月总结文档
- 文档格式为 Markdown，包含8个字段结构

**Obsidian**
- 本地知识管理工具，云环境无法直接访问
- 需要间接同步方案

### 1.2 核心挑战

1. **云环境限制**：无法直接访问用户本地 Obsidian vault
2. **双向同步复杂性**：需要处理冲突检测、版本控制
3. **内容格式一致性**：确保三端内容格式统一

---

## 二、打通架构设计

### 2.1 总体架构

```
┌─────────────┐     双向同步      ┌──────────────┐
│   悟空       │ ←──────────────→ │   钉钉文档    │
│ (云端工作空间) │                  │ (在线协作文档)  │
└──────┬──────┘                    └──────────────┘
       │
       │ Markdown 导出
       ↓
┌─────────────┐
│   Obsidian   │
│  (本地Vault)  │
└─────────────┘
```

### 2.2 数据流向

#### 方向1：悟空 → 钉钉文档（自动同步）
- **触发条件**：悟空生成新的工作日志/周报/月报
- **执行方式**：通过 `dws doc create` 或 `dws doc update`
- **同步内容**：
  - `work_logs/YYYY-MM-DD.md` → 钉钉文档 "YYYY-MM-DD 工作日志"
  - `weekly_summaries/YYYY-WW_周总结.md` → 钉钉文档 "第WW周周总结"
  - `monthly_summaries/YYYY-MM_月总结.md` → 钉钉文档 "YYYY年MM月月总结"

#### 方向2：钉钉文档 → 悟空（定期拉取）
- **触发条件**：定时任务（每天20:00）或手动触发
- **执行方式**：通过 `dws doc search` + `dws doc read`
- **同步内容**：从钉钉文档拉取最新内容到本地

#### 方向3：悟空 → Obsidian（间接同步）
- **触发条件**：用户手动导出或定时生成导出包
- **执行方式**：生成标准 Markdown 文件包，用户通过以下方式导入：
  - Git 仓库同步
  - 网盘同步（OneDrive/Dropbox/坚果云）
  - 手动复制粘贴

#### 方向4：Obsidian → 悟空（间接同步）
- **触发条件**：用户在 Obsidian 中编辑后上传
- **执行方式**：
  - 用户上传 Markdown 文件到指定目录
  - 或通过 Git push 到共享仓库
  - 悟空定期拉取更新

---

## 三、详细实施方案

### 3.1 悟空 ↔ 钉钉文档双向同步

#### 3.1.1 推送到钉钉（悟空 → 钉钉）

**步骤1：检查文档是否存在**
```bash
dws doc search --query "2026-07-04 工作日志" --format json
```

**步骤2a：如果文档不存在，创建新文档**
```bash
dws doc create --name "2026-07-04 工作日志" --parent-node-id "<父节点ID>" --format json
```

**步骤2b：如果文档存在，更新内容**
```bash
dws doc update --node "<nodeId>" --markdown-file "work_logs/2026-07-04.md" --format json
```

#### 3.1.2 从钉钉拉取（钉钉 → 悟空）

**步骤1：搜索目标文档**
```bash
dws doc search --query "2026-07-04 工作日志" --format json
```

**步骤2：读取文档内容**
```bash
dws doc read --node "<nodeId>" --format json | jq -r '.markdown' > work_logs/2026-07-04.md
```

**步骤3：版本对比（可选）**
- 比较本地文件与钉钉文档的修改时间
- 如果钉钉版本更新，则覆盖本地文件
- 如果本地版本更新，则询问用户是否推送

### 3.2 悟空 → Obsidian 间接同步方案

由于云环境无法直接访问本地 Obsidian，提供以下三种方案：

#### 方案A：Git 仓库同步（推荐）

**优点**：
- 版本控制完整
- 冲突解决机制成熟
- 可追溯历史变更

**实施步骤**：
1. 在悟空工作空间初始化 Git 仓库
2. 配置远程仓库（GitHub/Gitee/私有Git服务器）
3. 用户本地 Obsidian vault 也关联同一远程仓库
4. 悟空定期 commit & push 更新
5. 用户本地 pull 获取更新

**自动化脚本**：
```bash
#!/bin/bash
# sync_to_obsidian.sh
cd /path/to/workspace
git add work_logs/ weekly_summaries/ monthly_summaries/
git commit -m "Auto-sync: $(date '+%Y-%m-%d %H:%M')"
git push origin main
```

#### 方案B：网盘同步文件夹

**优点**：
- 配置简单
- 无需技术背景

**实施步骤**：
1. 创建同步目录 `/sync_for_obsidian/`
2. 悟空将最新文件复制到该目录
3. 用户配置网盘客户端（OneDrive/坚果云）同步该目录到本地
4. Obsidian 打开该同步目录作为 vault 或子目录

#### 方案C：Markdown 导出包

**优点**：
- 最简单
- 适合偶尔同步

**实施步骤**：
1. 悟空将所有需要同步的文件打包成 zip
2. 用户下载并解压到 Obsidian vault
3. 手动合并冲突（如果有）

### 3.3 Obsidian → 悟空 间接同步方案

#### 方案A：Git Pull（配合方案A使用）

```bash
#!/bin/bash
# sync_from_obsidian.sh
cd /path/to/workspace
git pull origin main
# 检查哪些文件被更新
git diff --name-only HEAD@{1} HEAD
```

#### 方案B：上传接口

提供一个 Web 接口或钉钉表单，用户可以：
1. 上传 Markdown 文件
2. 填写同步说明
3. 系统自动将文件保存到对应目录

---

## 四、统一索引映射文件

创建 `sync_index.json` 文件，记录三端内容的对应关系：

```json
{
  "version": "1.0",
  "last_sync": "2026-07-04T20:00:00+08:00",
  "mappings": {
    "daily_logs": {
      "2026-07-03": {
        "wukong_path": "work_logs/2026-07-03.md",
        "dingtalk_node_id": "14lgGw3P8vvOePMpig2v92p285daZ90D",
        "dingtalk_url": "https://alidocs.dingtalk.com/i/nodes/14lgGw3P8vvOePMpig2v92p285daZ90D",
        "obsidian_path": "Daily Notes/2026-07-03.md",
        "last_modified_wukong": "2026-07-04T10:35:00+08:00",
        "last_modified_dingtalk": "2026-07-04T10:35:00+08:00",
        "last_modified_obsidian": null,
        "sync_status": "synced"
      },
      "2026-07-04": {
        "wukong_path": "work_logs/2026-07-04.md",
        "dingtalk_node_id": null,
        "dingtalk_url": null,
        "obsidian_path": "Daily Notes/2026-07-04.md",
        "last_modified_wukong": "2026-07-04T20:37:00+08:00",
        "last_modified_dingtalk": null,
        "last_modified_obsidian": null,
        "sync_status": "pending_push_to_dingtalk"
      }
    },
    "weekly_summaries": {
      "2026-W28": {
        "wukong_path": "weekly_summaries/2026-W28_周总结.md",
        "dingtalk_node_id": "NZQYprEoWoeR2w7Dc0Eq5BkkJ1waOeDk",
        "dingtalk_url": "https://alidocs.dingtalk.com/i/nodes/NZQYprEoWoeR2w7Dc0Eq5BkkJ1waOeDk",
        "obsidian_path": "Weekly Reviews/2026-W28.md",
        "last_modified_wukong": null,
        "last_modified_dingtalk": "2025-10-09T00:00:00+08:00",
        "last_modified_obsidian": null,
        "sync_status": "pending_pull_from_dingtalk"
      }
    },
    "monthly_summaries": {}
  }
}
```

**索引文件作用**：
1. 快速查找某篇文档在三端的对应位置
2. 追踪最后修改时间，判断哪个版本最新
3. 记录同步状态，避免重复同步
4. 支持增量同步，只同步有变化的内容

---

## 五、自动化同步脚本

### 5.1 主同步脚本 `sync_all.py`

```python
#!/usr/bin/env python3
"""
悟空-钉钉-Obsidian 三端同步脚本
功能：
1. 悟空 → 钉钉：推送新增/更新的文档
2. 钉钉 → 悟空：拉取钉钉文档更新
3. 生成 Obsidian 同步包
4. 更新索引文件
"""

import json
import os
import subprocess
from datetime import datetime, timedelta
from pathlib import Path

WORKSPACE = Path("/home/wuying/.kinto-agent/workspace/projects/工作总结")
INDEX_FILE = WORKSPACE / "sync_index.json"

def load_index():
    """加载索引文件"""
    if INDEX_FILE.exists():
        with open(INDEX_FILE, 'r', encoding='utf-8') as f:
            return json.load(f)
    return {"version": "1.0", "last_sync": None, "mappings": {}}

def save_index(index):
    """保存索引文件"""
    index["last_sync"] = datetime.now().isoformat()
    with open(INDEX_FILE, 'w', encoding='utf-8') as f:
        json.dump(index, f, ensure_ascii=False, indent=2)

def search_dingtalk_doc(query):
    """搜索钉钉文档"""
    cmd = f'dws doc search --query "{query}" --format json'
    result = subprocess.run(cmd, shell=True, capture_output=True, text=True)
    if result.returncode == 0:
        return json.loads(result.stdout)
    return None

def read_dingtalk_doc(node_id):
    """读取钉钉文档内容"""
    cmd = f'dws doc read --node "{node_id}" --format json'
    result = subprocess.run(cmd, shell=True, capture_output=True, text=True)
    if result.returncode == 0:
        data = json.loads(result.stdout)
        return data.get("markdown", "")
    return None

def create_dingtalk_doc(name, parent_node_id=None):
    """创建钉钉文档"""
    cmd = f'dws doc create --name "{name}" --format json'
    if parent_node_id:
        cmd += f' --parent-node-id "{parent_node_id}"'
    result = subprocess.run(cmd, shell=True, capture_output=True, text=True)
    if result.returncode == 0:
        return json.loads(result.stdout)
    return None

def update_dingtalk_doc(node_id, markdown_file):
    """更新钉钉文档"""
    cmd = f'dws doc update --node "{node_id}" --markdown-file "{markdown_file}" --format json'
    result = subprocess.run(cmd, shell=True, capture_output=True, text=True)
    return result.returncode == 0

def sync_daily_logs_to_dingtalk(index):
    """同步工作日志到钉钉"""
    logs_dir = WORKSPACE / "work_logs"
    if not logs_dir.exists():
        return
    
    for log_file in sorted(logs_dir.glob("*.md")):
        date_str = log_file.stem  # YYYY-MM-DD
        query = f"{date_str} 工作日志"
        
        # 检查索引中是否已有记录
        mapping = index["mappings"].get("daily_logs", {}).get(date_str, {})
        
        # 搜索钉钉文档
        search_result = search_dingtalk_doc(query)
        node_id = None
        
        if search_result and search_result.get("documents"):
            # 找到匹配的文档
            for doc in search_result["documents"]:
                if doc["name"] == query:
                    node_id = doc["nodeId"]
                    break
        
        if node_id:
            # 文档存在，检查是否需要更新
            if mapping.get("last_modified_wukong"):
                wukong_mtime = os.path.getmtime(log_file)
                # 简化：总是更新（实际应该比较时间戳）
                print(f"更新钉钉文档: {query}")
                update_dingtalk_doc(node_id, str(log_file))
        else:
            # 文档不存在，创建新文档
            print(f"创建钉钉文档: {query}")
            create_result = create_dingtalk_doc(query)
            if create_result:
                node_id = create_result.get("nodeId")
        
        # 更新索引
        if "daily_logs" not in index["mappings"]:
            index["mappings"]["daily_logs"] = {}
        
        index["mappings"]["daily_logs"][date_str] = {
            "wukong_path": f"work_logs/{log_file.name}",
            "dingtalk_node_id": node_id,
            "dingtalk_url": f"https://alidocs.dingtalk.com/i/nodes/{node_id}" if node_id else None,
            "obsidian_path": f"Daily Notes/{date_str}.md",
            "last_modified_wukong": datetime.fromtimestamp(os.path.getmtime(log_file)).isoformat(),
            "sync_status": "synced" if node_id else "pending_create"
        }

def sync_from_dingtalk_to_wukong(index):
    """从钉钉拉取文档到悟空"""
    # 示例：拉取最近7天的工作日志
    today = datetime.now()
    for i in range(7):
        date = today - timedelta(days=i)
        date_str = date.strftime("%Y-%m-%d")
        query = f"{date_str} 工作日志"
        
        search_result = search_dingtalk_doc(query)
        if search_result and search_result.get("documents"):
            for doc in search_result["documents"]:
                if doc["name"] == query:
                    node_id = doc["nodeId"]
                    markdown = read_dingtalk_doc(node_id)
                    
                    if markdown:
                        log_file = WORKSPACE / "work_logs" / f"{date_str}.md"
                        with open(log_file, 'w', encoding='utf-8') as f:
                            f.write(markdown)
                        print(f"已拉取: {date_str}")
                        
                        # 更新索引
                        if "daily_logs" not in index["mappings"]:
                            index["mappings"]["daily_logs"] = {}
                        
                        index["mappings"]["daily_logs"][date_str] = {
                            "wukong_path": f"work_logs/{date_str}.md",
                            "dingtalk_node_id": node_id,
                            "dingtalk_url": doc.get("docUrl"),
                            "obsidian_path": f"Daily Notes/{date_str}.md",
                            "last_modified_dingtalk": datetime.now().isoformat(),
                            "sync_status": "synced"
                        }
                    break

def generate_obsidian_sync_package(index):
    """生成 Obsidian 同步包"""
    sync_dir = WORKSPACE / "sync_for_obsidian"
    sync_dir.mkdir(exist_ok=True)
    
    # 复制最新的工作日志
    logs_dir = WORKSPACE / "work_logs"
    if logs_dir.exists():
        for log_file in sorted(logs_dir.glob("*.md"))[-7:]:  # 最近7天
            dest = sync_dir / "Daily Notes" / log_file.name
            dest.parent.mkdir(exist_ok=True)
            import shutil
            shutil.copy2(log_file, dest)
    
    # 复制周报和月报
    for subdir in ["weekly_summaries", "monthly_summaries"]:
        src_dir = WORKSPACE / subdir
        if src_dir.exists():
            dest_dir = sync_dir / ("Weekly Reviews" if "weekly" in subdir else "Monthly Reviews")
            dest_dir.mkdir(exist_ok=True)
            import shutil
            for file in src_dir.glob("*.md"):
                shutil.copy2(file, dest_dir / file.name)
    
    # 复制索引文件
    import shutil
    shutil.copy2(INDEX_FILE, sync_dir / "sync_index.json")
    
    print(f"已生成 Obsidian 同步包: {sync_dir}")

def main():
    print("=" * 60)
    print("悟空-钉钉-Obsidian 三端同步开始")
    print("=" * 60)
    
    # 加载索引
    index = load_index()
    
    # 步骤1：悟空 → 钉钉
    print("\n[1/3] 同步悟空工作日志到钉钉...")
    sync_daily_logs_to_dingtalk(index)
    
    # 步骤2：钉钉 → 悟空
    print("\n[2/3] 从钉钉拉取最新文档...")
    sync_from_dingtalk_to_wukong(index)
    
    # 步骤3：生成 Obsidian 同步包
    print("\n[3/3] 生成 Obsidian 同步包...")
    generate_obsidian_sync_package(index)
    
    # 保存索引
    save_index(index)
    
    print("\n" + "=" * 60)
    print("同步完成！")
    print(f"索引文件已更新: {INDEX_FILE}")
    print("=" * 60)

if __name__ == "__main__":
    main()
```

### 5.2 定时任务配置

使用 `cron_use` 创建定时任务：

```bash
# 每天20:00执行同步
cron_use action=add \
  --name "三端同步-每日工作日志" \
  --schedule "0 20 * * *" \
  --payload.prompt "执行 /home/wuying/.kinto-agent/workspace/projects/工作总结/sync_all.py 脚本，完成悟空-钉钉-Obsidian三端同步"
```

---

## 六、使用指南

### 6.1 首次配置

1. **初始化 Git 仓库**（如果使用方案A）
   ```bash
   cd /home/wuying/.kinto-agent/workspace/projects/工作总结
   git init
   git remote add origin <your-git-repo-url>
   git add .
   git commit -m "Initial commit"
   git push -u origin main
   ```

2. **配置 Obsidian**
   - 在本地创建 Obsidian vault
   - 克隆 Git 仓库到 vault 目录
   - 或配置网盘同步文件夹

3. **运行首次同步**
   ```bash
   python sync_all.py
   ```

### 6.2 日常使用

**场景1：悟空生成新日志后自动同步到钉钉**
- 悟空生成 `work_logs/2026-07-04.md`
- 运行 `sync_all.py` 或等待定时任务执行
- 脚本自动创建/更新钉钉文档

**场景2：在钉钉编辑文档后同步回悟空**
- 在钉钉文档中编辑内容
- 运行 `sync_all.py` 的拉取部分
- 或直接执行：
  ```bash
  dws doc read --node "<nodeId>" --format json | jq -r '.markdown' > work_logs/2026-07-04.md
  ```

**场景3：同步到 Obsidian**
- 运行 `sync_all.py` 生成同步包
- 如果是 Git 方案：在本地 vault 执行 `git pull`
- 如果是网盘方案：等待网盘自动同步
- 如果是手动方案：下载 zip 包并解压

### 6.3 冲突处理

当三端内容不一致时：
1. 查看 `sync_index.json` 中的 `last_modified_*` 字段
2. 选择最新的版本作为基准
3. 手动合并其他版本的更改
4. 更新索引文件

---

## 七、注意事项

### 7.1 已知限制

1. **云环境无法直接操作 Obsidian**：必须通过间接方式（Git/网盘/手动）
2. **钉钉文档 API 限制**：单次批量操作不超过30条记录
3. **版本冲突**：需要人工介入处理复杂冲突

### 7.2 最佳实践

1. **单一编辑源原则**：尽量只在一个平台编辑，其他平台作为只读副本
2. **定期同步**：建议每天至少同步一次，避免差异过大
3. **备份重要文档**：同步前备份关键文档
4. **使用索引文件**：始终通过 `sync_index.json` 追踪同步状态

### 7.3 故障排查

**问题1：钉钉文档搜索不到**
- 检查文档标题格式是否正确
- 尝试不同的搜索关键词
- 确认有权限访问该文档

**问题2：同步失败**
- 检查网络连接
- 确认 dws 命令可用
- 查看详细错误日志

**问题3：Obsidian 未收到更新**
- Git 方案：检查 `git pull` 是否成功
- 网盘方案：检查网盘客户端是否正常运行
- 手动方案：确认下载的文件路径正确

---

## 八、后续优化方向

1. **Webhook 实时同步**：监听钉钉文档变更事件，实时触发同步
2. **智能冲突解决**：基于 diff 算法自动合并简单冲突
3. **多用户协作**：支持团队成员共同维护知识库
4. **AI 辅助整理**：利用 AI 自动分类、标签化文档内容
5. **可视化看板**：展示同步状态、文档统计等信息

---

*文档版本：1.0*
*最后更新：2026-07-04*
*维护者：柴建化*
